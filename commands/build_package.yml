---
version: 2.1

description: |
  Builds the given local package with command
  `opam install . --with-test -y`

steps:
  - run:
      name: Build project package
      command: |
        opam install . --with-test -y
